<?xml version="1.0" encoding="utf-8"?>
<!--
    SPDX-FileCopyrightText: 2023-2026 Patrick Gaskin
    SPDX-License-Identifier: AGPL-3.0-or-later
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <uses-feature
        android:name="android.software.live_wallpaper"
        android:required="true" />

    <uses-feature
        android:glEsVersion="0x30001"
        android:required="true" />

    <application
        android:icon="@mipmap/ic_launcher"
        android:label="Windy Live Wallpaper">

        <activity
            android:name="net.pgaskin.windy.LocationActivity"
            android:theme="@style/Theme.Transparent" />

        <service
            android:name="net.pgaskin.windy.WindFieldUpdateService"
            android:permission="android.permission.BIND_JOB_SERVICE" />

        <!--
            The per-theme wallpaper <service> entries are generated from
            core/src/config.rs by the :app:generate*Themes Gradle task (see
            app/build.gradle) and merged in from a generated manifest overlay.
        -->
    </application>

</manifest>